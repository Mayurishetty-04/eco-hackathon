# Dart/Flutter Analysis Options — EcoTrack
# Based on flutter_lints with additional rules for clean architecture

include: package:flutter_lints/flutter.yaml

analyzer:
  errors:
    # Treat missing returns as errors
    missing_return: error
    # Unused imports are warnings
    unused_import: warning
    # Unused local variables
    unused_local_variable: warning
    # Dead code
    dead_code: warning

  exclude:
    # Exclude generated files from analysis
    - "**/*.g.dart"
    - "**/*.freezed.dart"
    - "lib/generated/**"

linter:
  rules:
    # Code organization
    directives_ordering: true
    sort_constructors_first: true
    always_declare_return_types: true

    # Naming
    file_names: true
    non_constant_identifier_names: true
    constant_identifier_names: true

    # Style
    prefer_const_constructors: true
    prefer_const_declarations: true
    prefer_final_fields: true
    prefer_final_locals: true
    prefer_single_quotes: true

    # Safety
    avoid_print: true             # Use logger package instead
    avoid_empty_else: true
    avoid_returning_null_for_void: true
    cancel_subscriptions: true
    close_sinks: true

    # Flutter-specific
    use_key_in_widget_constructors: true
    sized_box_for_whitespace: true
    use_full_hex_values_for_flutter_colors: true
