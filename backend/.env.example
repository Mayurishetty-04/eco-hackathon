# Backend Environment Variables
# ================================
# Copy this file to .env:
#   copy .env.example .env     (Windows)
#   cp .env.example .env       (Mac/Linux)
#
# ⚠️  ALL teammates must use the SAME port (5000)
#     so Flutter can connect to the same server.
# ================================

PORT=5000
